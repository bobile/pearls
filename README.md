# 🎵 Музыкальные жемчужины 🎵

Веб-страница с коллекцией музыкальных артистов и их треков с встроенными YouTube-плеерами.

## Описание

Страница содержит информацию о различных электронных музыкантах с:
- Кратким описанием каждого артиста
- Фотографиями
- Встроенными YouTube-плеерами для прослушивания треков прямо на странице

## Как опубликовать на GitHub Pages

1. Создайте новый репозиторий на GitHub (или используйте существующий)

2. Загрузите все файлы в репозиторий:
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin https://github.com/ВАШ_USERNAME/pearls.git
   git push -u origin main
   ```

3. В настройках репозитория перейдите в раздел **Pages** (Settings → Pages)

4. В разделе **Source** выберите:
   - Branch: `main` (или `master`)
   - Folder: `/ (root)`

5. Нажмите **Save**

6. Ваш сайт будет доступен по адресу:
   `https://ВАШ_USERNAME.github.io/pearls/`

## Структура файлов

```
pearls/
├── index.html          # Главная страница
├── README.md           # Этот файл
├── Source.md           # Исходные данные
└── *.jpeg, *.jpg       # Изображения артистов
```

## Локальный просмотр

Для локального просмотра просто откройте `index.html` в браузере или используйте локальный сервер:

```bash
# Python 3
python -m http.server 8000

# Node.js (если установлен http-server)
npx http-server
```

Затем откройте в браузере: `http://localhost:8000`

## Технологии

- Чистый HTML/CSS
- Встроенные YouTube-плееры
- Адаптивный дизайн
